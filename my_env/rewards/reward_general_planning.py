import torch
import my_env

def reward_general_planning(self: "my_env.layout_env.LayoutEnv", action: torch.tensor) -> torch.Tensor:
    """在最后一次布置完成后，返回最终布局奖励"""

    # todo 尚未完成

    return torch.tensor(0)